import {
  Bell,
  Command,
  Search,
  type LucideIcon,
  PlusCircle,
} from "lucide-react";

export type Icon = LucideIcon;

export const Icons = {
  bell: Bell,
  command: Command,
  search: Search,
  add: PlusCircle,
};
